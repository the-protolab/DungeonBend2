{
  "name": "dungeon-bend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:bend": "bun run scripts/build-bend.ts",
    "dev": "bun run build:bend && vite",
    "build": "bun run build:bend && vite build",
    "build:pages": "vite build",
    "preview": "vite preview",
    "test": "bun run build:bend && bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^5.4.14"
  }
}
